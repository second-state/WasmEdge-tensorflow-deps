set(TENSORFLOW_INCLUDE
  "${CMAKE_CURRENT_SOURCE_DIR}"
  CACHE INTERNAL "${TENSORFLOW_INCLUDE}"
)
set(JPEG_INCLUDE
  "${CMAKE_CURRENT_SOURCE_DIR}/jpeg"
  CACHE INTERNAL "${JPEG_INCLUDE}"
)
set(PNG_INCLUDE
  "${CMAKE_CURRENT_SOURCE_DIR}/png"
  CACHE INTERNAL "${PNG_INCLUDE}"
)